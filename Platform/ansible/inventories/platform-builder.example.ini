[platform_builders]
# Use the VM's private IP when the Ansible control host can reach it.
builder-1 ansible_host=10.0.0.20 ansible_user=ubuntu

[platform_builders:vars]
ansible_python_interpreter=/usr/bin/python3
