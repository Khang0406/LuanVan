apiVersion: v1
kind: Service
metadata:
  name: {{ service_name }}
  namespace: {{ namespace }}
spec:
  type: NodePort
  selector:
    app: {{ service_name }}
  ports:
    - port: {{ port }}
      targetPort: {{ port }}
      nodePort: {{ node_port }}
